{
  "input": {
    "type": "object",
    "properties": {
      "project_name": {
        "type": "string",
        "description": "Name of the Cloudflare Pages project to retrieve (lowercase-hyphenated)",
        "x-semantic-type": "ProjectName"
      }
    },
    "required": ["project_name"],
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "name": { 
        "type": "string",
        "description": "Project name",
        "x-semantic-type": "ProjectName"
      },
      "url": { 
        "type": "string",
        "description": "Project base URL (https://{name}.pages.dev). Each deployment gets a unique subdomain.",
        "x-semantic-type": "CloudflareURL"
      },
      "created_at": { 
        "type": "string",
        "description": "ISO 8601 timestamp of project creation"
      }
    }
  }
}
