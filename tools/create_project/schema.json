{
  "input": {
    "type": "object",
    "properties": {
      "project_name": {
        "type": "string",
        "description": "Name of the Cloudflare Pages project (lowercase-hyphenated). E.g., 'my-site', 'company-blog'. Must be unique within account.",
        "x-semantic-type": "ProjectName",
        "pattern": "^[a-z][a-z0-9-]*$"
      }
    },
    "required": ["project_name"],
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "project_name": { 
        "type": "string",
        "description": "Created project name"
      },
      "url": { 
        "type": "string",
        "description": "Project base URL (https://{project_name}.pages.dev). Individual deployments get unique subdomains.",
        "x-semantic-type": "CloudflareURL"
      },
      "created_at": { 
        "type": "string",
        "description": "ISO 8601 timestamp of project creation"
      },
      "status": { 
        "type": "string",
        "enum": ["created", "existing"],
        "description": "Whether project was newly created or already existed (idempotent operation)"
      }
    }
  }
}
