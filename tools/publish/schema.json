{
  "input": {
    "type": "object",
    "properties": {
      "project_dir": {
        "type": "string",
        "description": "Absolute path to the project directory containing _site.yaml or _project.yaml.",
        "x-semantic-type": "ProjectDir"
      },
      "branch": {
        "type": "string",
        "description": "Optional preview branch name for staging deployments."
      }
    },
    "required": ["project_dir"],
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Deployment ID"
      },
      "url": {
        "type": "string",
        "description": "Live deployment URL",
        "x-semantic-type": "CloudflareURL"
      },
      "project_name": {
        "type": "string",
        "description": "Cloudflare Pages project name"
      }
    }
  }
}
