Preview a single component in isolation

Renders a single component section as a standalone HTML document, useful for testing and previewing individual components before adding them to a full page. Returns a complete HTML document with the component rendered.