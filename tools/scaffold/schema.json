{
  "input": {
    "type": "object",
    "properties": {
      "spec_path": {
        "type": "string",
        "description": "Absolute path to the Gutenberg page specification YAML file.",
        "x-semantic-type": "GutenbergSpecPath"
      }
    },
    "required": ["spec_path"],
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "scaffold_path": {
        "type": "string",
        "description": "Path to written {name}.scaffold.json artifact containing RenderNode tree.",
        "x-semantic-type": "ArtifactPath"
      },
      "section_count": {
        "type": "integer",
        "description": "Number of sections scaffolded"
      }
    }
  }
}
